<% content_for(:title) { "Edit #{@workspace.name} - Rails-Claw" } %>

<div class="max-w-lg mx-auto">
  <h1 class="text-2xl font-bold text-gray-900 mb-6">Edit Workspace</h1>
  <%= render "form", workspace: @workspace %>
</div>
