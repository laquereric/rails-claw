<nav class="bg-gray-800">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="flex h-16 items-center justify-between">
      <div class="flex items-center">
        <div class="shrink-0">
          <span class="text-white font-bold text-xl">Rails-Claw</span>
        </div>
        <div class="ml-10 flex items-baseline space-x-4">
          <%= link_to "Dashboard", root_path, class: "rounded-md px-3 py-2 text-sm font-medium #{current_page?(root_path) ? 'bg-gray-900 text-white' : 'text-gray-300 hover:bg-gray-700 hover:text-white'}" %>
          <%= link_to "Workspaces", workspaces_path, class: "rounded-md px-3 py-2 text-sm font-medium #{request.path.start_with?('/workspaces') ? 'bg-gray-900 text-white' : 'text-gray-300 hover:bg-gray-700 hover:text-white'}" %>
          <%= link_to "Inference", inference_path, class: "rounded-md px-3 py-2 text-sm font-medium #{request.path.start_with?('/inference') ? 'bg-gray-900 text-white' : 'text-gray-300 hover:bg-gray-700 hover:text-white'}" %>
          <%= link_to "Deployments", deployments_path, class: "rounded-md px-3 py-2 text-sm font-medium #{request.path.start_with?('/deployments') ? 'bg-gray-900 text-white' : 'text-gray-300 hover:bg-gray-700 hover:text-white'}" %>
        </div>
      </div>
    </div>
  </div>
</nav>
