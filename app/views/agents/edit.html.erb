<% content_for(:title) { "Edit #{@agent.name} - Rails-Claw" } %>

<div class="max-w-lg mx-auto">
  <h1 class="text-2xl font-bold text-gray-900 mb-6">Edit Agent</h1>
  <%= render "form", agent: @agent, workspace: @workspace %>
</div>
